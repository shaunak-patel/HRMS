import React from 'react';

const About = () => {
    return (
        <div>
            <h2>About Page</h2>
            <p>This is a simple React application that fetches data from an API.</p>
        </div>
    );
};

export default About;